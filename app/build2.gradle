// Project-level build.gradle
buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath "com.android.tools.build:gradle:8.x.x"
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:1.7.10" // Older version
    }
}